name: Deploy to Google Cloud VM

on:
  push:
    branches:
      - main   # change to "master" if that's your default branch

jobs:
  deploy:
    name: SSH & Restart Bot
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host:     ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key:      ${{ secrets.VM_SSH_KEY }}
          command_timeout: 5m
          script: |
            set -e

            echo "==> Navigating to bot directory..."
            cd ${{ secrets.VM_PATH }}

            echo "==> Pulling latest code..."
            git pull origin main

            echo "==> Installing/updating dependencies..."
            npm install --omit=dev --no-progress

            echo "==> Redeploying slash commands to Discord..."
            node src/deploy-guild.js || true

            echo "==> Restarting bot via pm2..."
            timeout 60 pm2 restart KozzyX --update-env || timeout 60 pm2 start index.js --name KozzyX || true
            timeout 15 pm2 save || true

            echo "==> PM2 status:"
            pm2 list

            echo "==> Done! Bot is back online."
